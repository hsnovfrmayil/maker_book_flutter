import 'package:flutter/material.dart';

class CommunucatAddPage extends StatelessWidget {
  const CommunucatAddPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(child: Text("CommunucatAddPage")),
    );
  }
}
