import 'package:flutter/material.dart';

class ReadingTargetPage extends StatelessWidget {
  const ReadingTargetPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(child: Text("ReadingTargetPage")),
    );
  }
}
