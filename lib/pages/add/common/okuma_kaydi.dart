import 'package:flutter/material.dart';

class ReadingRecordPage extends StatelessWidget {
  const ReadingRecordPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(child: Text("ReadingRecordPage")),
    );
  }
}
